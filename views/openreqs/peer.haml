%h2 Documents
%ul
  - @versions.each do |k,versions|
    %li
      = k
      %ul
        - versions.each do |v|
          %li= v
    
%form{:action => to("/a/peers/#{@name}/sync"), :method => "post"}
  %input#sync(type="submit" value="Sync")
